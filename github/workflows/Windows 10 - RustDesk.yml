services:

  wg-easy:
    image: ghcr.io/wg-easy/wg-easy
    container_name: wg-easy 
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    environment:
      - LANG=es
      - CONTRASEÑA=p4ssw0rd
      - WG_DEFAULT_DNS=1.1.1.1, 8.8.8.8
      - WG_HOST=205.134.180.147
      - WG_PORT=51820
    ports:
      - 51820:51820/udp
      - 51821:51821/tcp
    restart: unless-stopped
    sysctls:
      - net.ipv4.conf.all.src_valid_mark=1
      - net.ipv4.ip_forward=1
    volumes:
      - ./wireguard:/etc/wireguard

      - name: Time Counter
        run: python time.py
